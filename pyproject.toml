[project]
name = "risk-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "oskari-nerd", email = "oskari@koutajoki.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "yfinance>=1.7.0",
]

[project.scripts]
risk-monitor = "risk_monitor:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
